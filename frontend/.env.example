# Development only.
# Vite serves React from localhost:5173, so the browser needs the backend URL.
# In production/package mode this variable should be omitted so API calls are relative
# to the same FastAPI server that serves the React build.
VITE_API_BASE_URL=http://localhost:8000

# Development only.
# Vite serves React from localhost:5173, so browser API calls must go to FastAPI.
# Do not set this in packaged production mode; production uses relative API URLs.
VITE_API_BASE_URL=http://localhost:8000
